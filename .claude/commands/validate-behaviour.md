---
description: Run the requirements-validator agent against a specific 
  behavior, checking not just whether it computes correctly but 
  whether it's actually visible/enforced where it matters.
argument-hint: [what to check]
---
Use the requirements-validator agent to check: $ARGUMENTS
Specifically: don't just confirm the logic computes the right value — 
check whether the result is actually visible, displayed, or enforced 
wherever a real user or reviewer would encounter it.

